#include <stdio.h>

/**
 * main- Entry point
 *
 * always: return 0 (Success)
 *
 * pintf is not allowed
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");
	return(0);
}
